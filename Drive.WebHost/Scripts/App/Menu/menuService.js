(function () {
    'use strict';

    angular
        .module('driveApp')
        .factory('MenuService', MenuService);

    MenuService.$inject = [];

    function MenuService() {
        var service = {
            
        };

        return service;
    }
})();